import styled from "styled-components/macro";

export default function NavigationBar() {
    return (
        <Navigation>Navigation (comes later)</Navigation>
    )
}

const Navigation = styled.form`
  background: #06adef;
`